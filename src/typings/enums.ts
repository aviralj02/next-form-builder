export enum ButtonType {
  ACTIVE = "active",
  SUBMIT = "submit",
}

export enum QuestionType {
  SHORT_ANS = "short_answer",
  LONG_ANS = "long_answer",
  NUMBER = "number",
  SINGLE_SELECT = "single_select",
  URL = "url",
  DATE = "date",
}
